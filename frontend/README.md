# Academy Frontend

Run the frontend:

```bash
cd frontend
npm install
npm run dev
```

App routes:
- `/login` — login page
- `/` — dashboard
- `/notes` — notes
- `/past-questions` — past questions
- `/cbt` — CBT quiz
